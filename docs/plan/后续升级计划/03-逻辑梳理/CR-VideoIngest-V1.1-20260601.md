# 澄清请求清单 — VideoIngest V1.1（SA-001 终版）

> **生成方**：SA-001
> **日期**：2026-06-01
> **总条数**：1（仅 Q-008；SA 不再产生新阻塞澄清，全部由 CLI 启动时自动检测解决）
> **阻塞状态**：1 条可绕过继续

---

## 澄清请求详表

| 编号 | 关联 PRD | 歧义描述 | SA 建议方案 | 备选方案 | 需要确认问题 | 风险等级 | 阻塞状态 |
|------|----------|----------|------------|----------|--------------|----------|----------|
| CR-001 | F-003 / NF-008 / Q-008 | Deno 运行时是否已在用户机器（Win11 / macOS 14+ / Ubuntu 22.04+）预装？| [SA建议] **方案 A（推荐）**：CLI 启动时自动检测 `deno --version`，缺失时打印 `curl -fsSL https://deno.land/install.sh | sh` 并返回 E_DL_001_DENO_MISSING；不阻塞 B 站任务，仅阻塞 YouTube 任务。 | **方案 B**：要求用户在首次安装时手动执行 Deno 安装；文档化安装命令；CLI 不做自动检测。 | 1) 是否同意方案 A（自动检测+友好提示）？ 2) Win11 是否含 Deno 默认安装路径？ | 高 | **可绕过继续**（CLI 自动检测方案已在 V1.1 PRD 落地，Q-008 风险已由 AC-E2E-08 缓解）|

---

## 阻塞分析

- **CR-001 不阻塞 SA 分析**——V1.1 PRD 已明确"CLI 启动时自动检测 + 缺失返回 E_DL_001_DENO_MISSING"（F-003.AC-2 + Q-008 + RR-001 + preflight 模块），SA 已据此完成 BP-002 preflight 设计。
- **CR-001 实际等待**第⑧棒首次端到端跑通时（CLI 自动检测）回写 Deno 预装统计。

---

> **本文件结束**。1 条澄清请求，均可绕过，SA 分析不阻塞。
