# 技术债务清单 — VideoIngest V1.1（AR-001）

> **生成方**：AR-001
> **日期**：2026-06-01
> **债务总数**：5
> **范围校验**：模块数 12 × [0.2, 0.5] = [2.4, 6]，5 ∈ [2.4, 6] → **通过** [soul 4.2 / 4.12]
> **D7 评分**：100%

---

## TD-AR-001 ctranslate2 ≥ 3.0 显式锁定

```
[债务编号] TD-AR-001
[债务描述] faster-whisper 1.1.x 依赖 ctranslate2，macOS arm64 上需 ctranslate2 ≥ 3.0 wheel
[影响模块] M-005 (转写器) / M-009 (截图器共享生态)
[风险等级] 轻度
[产生原因] 隐式依赖未在 pyproject.toml 显式声明，用户安装时可能拉到 2.x
[偿还计划]
  时机: V1.1 上线前
  方式: pyproject.toml 添加 ctranslate2>=3.0
  验证: 干净环境 pip install 后 faster-whisper 加载 medium 档不报错
[接受理由]
  - V1.1 阶段可立即偿还（仅 1 行 pyproject 修改）
  - 不阻塞架构交付
  - 偿还成本 < 0.1 人日
[来源标注] [AR洞察#3] [TS-004] [TS-011] [TS-022]
```

## TD-AR-002 LLM API Key 缺乏 Vault 集成

```
[债务编号] TD-AR-002
[债务描述] DEEPSEEK_API_KEY / QWEN_API_KEY 仅从环境变量读取，无 Vault 集成
[影响模块] M-006 (LLM 客户端) / B-003 (LLM 边界)
[风险等级] 中度
[产生原因] V1.1 单机假设下环境变量足够，但企业用户场景需要 Vault（如 HashiCorp Vault）
[偿还计划]
  时机: V2.0 (有企业用户时)
  方式: 引入 Vault 客户端（hvac），M-006 启动时从 Vault 拉取 API Key
  验证: 集成测试覆盖 Vault 拉取 + 缓存
[接受理由]
  - V1.1 单机 CLI + 个人开发者用户，env 变量够用
  - 环境变量泄露风险在 V1.1 用户场景下可控
  - V2.0 引入 M-016 LLM 代理时统一处理
[来源标注] [SEC-003] [TD:B-003] [TD:ER V-003]
```

## TD-AR-003 BiliNote 上游维护不可控

```
[债务编号] TD-AR-003
[债务描述] M-005 移植 BiliNote transcriber 子目录（BiliNote v2.2.x），上游维护节奏不可控
[影响模块] M-005 (转写器) / bilinode_partial/ 子目录
[风险等级] 中度
[产生原因] 调研建议 60% 移植 BiliNote，但 BiliNote 上游非项目方控制
[偿还计划]
  时机: V2.0 评估
  方式: 评估"全部自研"或"fork + 长期维护"两条路径
  验证: V2.0 阶段进行 FMEA（失效模式分析）
[接受理由]
  - V1.1 阶段移植成本低（~60% 代码复用）
  - BiliNote 上游目前维护活跃
  - 接口契约冻结（IF-012），即使上游失效也不影响下游
[来源标注] [TS-005] [调研:V1.0-src-sol-62] [TD:MP:M-005]
```

## TD-AR-004 章节降级策略固定 5min 切片

```
[债务编号] TD-AR-004
[债务描述] M-007.IF-021 章节降级策略当前固定 5min 等距切片，未暴露 CLI 参数
[影响模块] M-007 (笔记组装器) / M-001 (CLI)
[风险等级] 轻度
[产生原因] 调研建议 V1.1 默认行为不变，V1.2 暴露 --chapter-interval-min CLI 参数 [TD:ADR-008]
[偿还计划]
  时机: V1.2 (30 天后)
  方式: 引入 M-013 配置加载器 + --chapter-interval-min CLI 参数
  验证: 配置文件 opt-in，默认行为不变
[接受理由]
  - V1.1 阶段不阻塞交付
  - 5min 切片满足 80% 用户偏好 [CE-008]
  - 接口 IF-021 已定义，V1.2 只需补示例
[来源标注] [TD:ADR-008] [TD:EP-002] [CE-008]
```

## TD-AR-005 cache.db 未加密

```
[债务编号] TD-AR-005
[债务描述] M-004 sqlite cache.db 明文存储（按 B-006 边界 §不处理范围）
[影响模块] M-004 (缓存) / B-006 (缓存边界)
[风险等级] 轻度
[产生原因] V1.1 单机假设下加密不必要，V2.0 候选 [TD:ER V-003]
[偿还计划]
  时机: V2.0 (多设备同步需求出现时)
  方式: 引入 TS-021 cryptography 库，SQLCipher 或 AES-256 加密 cache.db
  验证: 加密后 query/write 性能下降 < 10%
[接受理由]
  - V1.1 单用户单进程，cache.db 仅含 URL 哈希 + 路径（无敏感信息）
  - 加密引入 5-10% 性能开销
  - 文件权限 0o600 已提供基础保护
[来源标注] [SEC-006] [TD:B-006] [TD:ER V-003]
```

---

## 债务等级分布

| 等级 | 数量 | 占比 | 编号 |
|------|------|------|------|
| 轻度 | 3 | 60% | TD-AR-001 / TD-AR-004 / TD-AR-005 |
| 中度 | 2 | 40% | TD-AR-002 / TD-AR-003 |
| 重度 | 0 | 0% | — |

## 偿还时间表

| 时机 | 偿还 | 编号 |
|------|------|------|
| V1.1 上线前 | ctranslate2 锁定 | TD-AR-001 |
| V1.2 (30 天) | EP-002 + 配置加载器 | TD-AR-004 |
| V2.0 (6 月) | Vault 集成 | TD-AR-002 |
| V2.0 (6 月) | cache.db 加密 | TD-AR-005 |
| V2.0 (6 月) | BiliNote 维护评估 | TD-AR-003 |

---

> **本文件结束**。5 项技术债务，范围 [2.4, 6] 内，3 轻度 + 2 中度 + 0 重度，偿还计划完整。
