# 收束报告 — research-tool v0.1.1

**收束节点**: 首次收束（全项目基线）  
**日期**: 2026-05  
**依据**: 开发规范 conventions/01-06+08 + ai-workflow 第三步·收束节点

---

## 阶段 1：整理报告

### 代码清理
| 检查项 | 结果 |
|--------|------|
| 密钥硬编码 | ✅ 已替换 config.yaml / _p2test_config.yaml 中的明文 API Key 为 `${DEEPSEEK_API_KEY}` / `${TAVILY_API_KEY}` |
| print() → logging | ✅ 全项目迁移至 logging 模块（cli.py、webui.py）；ruff T20 通过 |
| 裸 except Exception | ✅ ~35 处全部添加 `as exc` + `logger.debug()`；ruff S112 通过 |
| 死导入 | ✅ `stages/__init__.py` 移除 5 个不存在的函数级导入 |
| 注释代码 | ✅ 无遗留注释代码块（TODO 注释保留） |
| 超长函数 | ✅ `cli.py:run` 的 overrides 构建抽离至 `_build_run_overrides` |

### 基础设施新增
- `ruff` lint 配置（`pyproject.toml`）：E/F/N/T20/T100/S/PLR0915
- `.pre-commit-config.yaml`：ruff + gitleaks
- `logging_config.py`：统一日志模块（INFO→stdout 无时间戳，WARNING+→stderr 含戳）
- `CLAUDE.md`：AI 上下文（模块图谱 + 规范状态速查）
- `worklogs/decisions/0001-六阶段管道文件系统通信.md`：ADR 记录

### 文件整理
| 检查项 | 结果 |
|--------|------|
| worklogs/ | ✅ 新建 decisions/ 子目录 |
| CLAUDE.md | ✅ 已创建 |
| 过时文档 | N/A（项目无 docs/ 目录，文档均在 README.md） |

---

## 阶段 2：测试报告

| 测试类型 | 结果 |
|----------|------|
| 全量单元测试 | ✅ 92/92 通过（0.98s） |
| 覆盖模块 | 19 个测试文件：backward_pass/cache/cleaner/cleaner_p2/collector_dedup/collector_deep_year/collector_queries/config/deepen/deepen_p2/deepen_profile/filename/pdf_ingest/pipeline_mock/quality_filters/search_backends/search_filters/slug/translate |

---

## 阶段 3：审计报告

### 🔴 红线扫描
| 红线 | 状态 | 备注 |
|------|------|------|
| 密钥硬编码 (02-红线3) | ✅ 已修复 | config.yaml 全部替换为环境变量 |
| print() 代日志 (02-红线1) | ✅ 已修复 | 全项目 logging |
| 静默吞异常 (02-红线4) | ✅ 已修复 | 全部加 as exc + log |
| SQL 参数化 (02-红线2) | N/A | 项目无 SQL |
| 注释代码块 (02-红线5) | ✅ 通过 | 无遗留 |
| 日志脱敏 (02-红线6) | ✅ 通过 | API Key 打印时脱敏为 `***` |
| 外部输入校验 (02-红线7) | ✅ 通过 | Pydantic 模型校验 |

### 🟡 架构扫描
| 检测项 | 状态 | 备注 |
|--------|------|------|
| 循环依赖 | ✅ 无 | 单向依赖链 |
| 跨层调用 | 🟡 有意简化 | cli.py `_make_llm()` 直接构造 LLMClient；CLI 薄封装架构决策 |
| 领域层依赖框架 | ✅ 通过 | models.py/config.py/errors.py 仅依赖 pydantic 标准库 |

### 残余 lint（均为预存，非本次引入）
- E501：7 处行过长（中文 help 字符串/注释，可读性优先）
- S603：1 处 subprocess（MinerU CLI 调用，已知路径非用户输入）
- S314：1 处 xml 解析（Google News RSS，只读）
- PLR0915：2 处函数过长（deepen.run 52 行、webui.run_web 94 行，后续优化）

---

## 阶段 4：效果验证

### BDD 场景回演
| 场景 | 结果 |
|------|------|
| CLI 命令可用 | ✅ `research --help` 正常 |
| 配置加载 | ✅ 环境变量解析正常 |
| 全量测试 | ✅ 92/92 通过 |
| 日志输出 | ✅ INFO 级别干净输出，WARNING 含时间戳 |

### 文档一致性
| 检查项 | 结果 |
|--------|------|
| README 快速开始 | ✅ 一键 `start.bat` 流程未变 |
| CLAUDE.md 模块图谱 | ✅ 与 `meta/FILE_GRAPH.md` 一致 |
| docstring 公共 API | ✅ `__init__.py` 导出函数均有 docstring |

---

## 产物清单

| 产物 | 路径 |
|------|------|
| 收束报告 | `docs/reports/收束报告-v0.1.1.md`（本文件） |
| CLAUDE.md | `CLAUDE.md` |
| CODE_MAP.md | `CODE_MAP.md`（人读：Mermaid 架构图 + 逐模块调用表） |
| CHANGELOG.md | `CHANGELOG.md`（Keep a Changelog 格式） |
| STATUS.md | `STATUS.md`（功能点进度 + 技术债） |
| ADR | `worklogs/decisions/0001-六阶段管道文件系统通信.md` |
| ruff 配置 | `pyproject.toml` |
| pre-commit 配置 | `.pre-commit-config.yaml` |
| Git 提交模板 | `.gitmessage` |
| PR 模板 | `.github/pull_request_template.md` |
| commitlint 配置 | `commitlint.config.js` |
| .gitignore 更新 | `.codegraph/` `.understand-anything/` 排除 |
| 日志模块 | `research_tool/logging_config.py` |
| Git Tag | `v0.1.1` |

---

## 技术债（遗留项）

| 问题 | 优先级 | 预计修复 |
|------|--------|---------|
| E501 行过长（7 处） | 🟢 低 | 中文 help 字符串/注释，可读性优先 |
| PLR0915 函数过长（2 处） | 🟢 低 | deepen.run / webui.run_web |
| S603 subprocess 告警 | 🟢 低 | MinerU 调用已知路径 |
| 覆盖率报告 | 🟡 中 | 需 `pip install pytest-cov` |
| pre-commit 钩子安装 | 🟡 中 | 需 `pre-commit install` |
| 01-架构 importlinter | 🟢 低 | 轻量项目可裁剪 |
