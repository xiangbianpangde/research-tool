# 反例验证记录 — VideoIngest V1.1（SA-001 终版）

> **生成方**：SA-001
> **日期**：2026-06-01
> **覆盖要求**：8 个核心流程每个 ≥ 1 个失效反例
> **实际生成**：10 条（核心 8 + 标准 1 + 辅助 1，作为 SA 洞察补充）

---

## 反例详表

| 编号 | 关联流程 | 失效场景 | 根因分析 | 影响范围 | 当前处理能力 | 建议修复 |
|------|----------|----------|----------|----------|--------------|----------|
| CE-001 | BP-002 preflight | 用户启动时缺 Deno，随后中途安装 Deno，但 3 并发任务共享初始 PreflightReport（deno_pass=false），导致 YouTube 任务持续失败 | [SA洞察#1] 跨并发任务的共享状态时效性问题 | 3 个任务全部失败，用户体验差 | 部分能——preflight 缺 Deno 阻断，但无法感知"中途安装"| PreflightReport.ttl_seconds=60；每个任务在 BP-003 前重新拉取 preflight 状态 |
| CE-002 | BP-003 下载 | B 站 4K 视频第一次返回 403，用户提供 cookie 后第二次入参仍返回 E_DL_BILI_403（cookie 实际有效）| F-003 未明确 cookie 注入时机 + 错误码单一 | 用户误判 cookie 无效 | 不能——错误码未区分 cookie 缺失/cookie 无效/cookie 过期 | 增加 E_DL_BILI_403_COOKIE_INVALID 错误码子类型 |
| CE-003 | BP-003 下载 | YouTube PO Token 24h 内轮换 3 次，缓存 etag 全部失效，导致命中陈旧笔记 | [调研:RR-007] YouTube etag 频繁变更 | 笔记质量下降 | 部分能——双键校验可避免命中过期，但 24h 内多次重新下载 | BR-024：YouTube 视频 24h 内强制 revalidate（etag_revalidate_at 字段）|
| CE-004 | BP-004 缓存 | 用户编辑本地笔记后再次入参，缓存命中返回编辑前的旧笔记 | F-009 未考虑"用户本地修改"场景 | 用户困惑 | 不能——缓存仅看 etag 不感知本地修改 | 增加 `local_mtime` 字段作为第三键；或显式提示"检测到本地修改，请用 --no-cache"|
| CE-005 | BP-005 转写 | medium 档 Whisper 在 60 分钟长视频上 OOM（RAM 8G 边界）| F-014 自动降档触发条件不严密 | 长视频转写失败 | 部分能——降档可缓解，但 8G 边界机器仍有 OOM 风险 | 检测视频时长 > 30 分钟 + RAM ≤ 8G → 强制 base 档（不依赖 medium）|
| CE-006 | BP-006 LLM 总结 | LLM 输出的 front matter 字段名遗漏 `video_` 前缀（直接写 "title" 而非 "video_title"），污染 raw/ | [SA洞察#2] 字段名强校验缺失 | 既有管道解析异常 | 不能——BP-006 步骤 6 缺字段名校验 | BR-025：BP-006 步骤 6 后增加字段名前缀校验；缺失时 LLM 强制重试 1 次 |
| CE-007 | BP-006 LLM 总结 | LLM 收到纯音乐视频的空转写稿，幻觉出 3 个虚假章节（"开场"、"高潮"、"结尾"）| [RA推理:RR-006] LLM 空白输入幻觉 | 笔记内容失真 | 部分能——BP-007 降级可兜底，但 LLM 调用已浪费 1 次 | BP-006 调用前预检测 transcript 长度 < 100 字 → 跳过 LLM 直接走 BP-007 等距切片 |
| CE-008 | BP-007 章节降级 | 25 分钟视频 LLM 切出 2 章节，降级按 5 分钟等距切片得 5 章节，但用户实际期望"开场/中段/结尾"3 段 | 降级策略固定 5 分钟，未考虑用户偏好 | 笔记结构不直观 | 不能——降级是默认策略 | 暴露 `--chapter-interval-min` CLI 参数让用户自定义 |
| CE-009 | BP-010 落盘 | 5 阶段管道 Collect 阶段未配置"raw/ 下发现 video_*.md 即纳入"，导致新视频笔记未被自动纳入 | F-006 验收 (1) "5 阶段自动跑通"未细化"配置 1 行"细节 | 管道不自动跑 | 不能——BP-010 步骤 4 缺配置注入 | 在 BP-010 步骤 4 显式调用 research-tool Collect 配置 API 注入 1 行配置 |
| CE-010 | BP-012 错误码 | 3 并发任务中 1 个 E_DL_001、1 个 E_DL_BILI_403、1 个成功，CLI 退出码错乱（返回 0 表示成功）| [SA推断:EX-039] 并发退出码仲裁未实现 | CI 误判为成功 | 不能——缺仲裁逻辑 | 退出码取最严重错误码（403 > 401 > 500 > 0）|

---

## 反例覆盖统计

| 流程等级 | 流程数 | 反例数 | 覆盖率 | 备注 |
|----------|--------|--------|--------|------|
| 核心 | 8 | 9 | 100% | BP-002/003/004/005/006/007/010/012 各 ≥ 1 |
| 标准 | 6 | 1 | 17% | BP-007（同时归属核心与标准）补充 1 条 |
| 辅助 | 2 | 0 | 0% | 辅助流程不强制反例 |

**核心流程反例覆盖率 = 100%**（满足 soul R10 + 6.2 交付检查）。

---

## 修复建议汇总（按反例编号）

- CE-001：PreflightReport.ttl_seconds=60，BP-003 前重新拉取
- CE-002：增加 E_DL_BILI_403_COOKIE_INVALID 子类型
- CE-003：BR-024 YouTube 24h revalidate
- CE-004：增加 `local_mtime` 字段
- CE-005：长视频 + 低 RAM 强制 base 档
- CE-006：BR-025 BP-006 步骤 6 字段名前缀强校验
- CE-007：BP-006 调用前预检测 transcript 长度
- CE-008：暴露 `--chapter-interval-min` CLI 参数
- CE-009：BP-010 步骤 4 显式注入 Collect 配置
- CE-010：CLI 退出码取最严重错误码

---

> **本文件结束**。10 条反例全部含根因 + 影响 + 修复建议。
