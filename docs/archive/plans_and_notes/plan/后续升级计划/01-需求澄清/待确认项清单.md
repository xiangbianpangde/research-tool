# 待确认项清单（VideoIngest V1.1 终版）

> **生成方**：PM-001
> **日期**：2026-06-01
> **使用方**：SA-001（设计引用）/ 第⑧棒（CLI 启动时引导）
> **总条数**：1（Q-008 新增，V1.0 7 条已全部闭环）
> **闭环率**：V1.0 7 条 → 7/7 = 100%；V1.1 新增 1 条待确认项

---

## 待确认项登记

### Q-008 [V1.1新增] — Deno 运行时是否已在用户机器预装？

| 字段 | 内容 |
|------|------|
| 风险等级 | **高** |
| 关联功能 | F-003, NF-008 |
| 关联调研 | R-002 闭环后衍生 |
| PM 默认假设 | 默认不预装；CLI 启动时自动检查并打印安装命令 |
| 关键不确定点 | (1) Windows 11 默认是否含 Deno？(2) macOS/Linux 用户是否需要独立安装？(3) 安装命令统一性？ |
| 等待谁确认 | 第⑧棒 CLI 首次启动时自动检测；用户确认安装命令 |
| 回写形式 | CLI 输出 `deno --version` 检测结果 + 缺失时打印 `curl -fsSL https://deno.land/install.sh | sh` |
| 失败影响 | 返回 `E_DL_001_DENO_MISSING`；CLI 拒绝继续执行 YouTube 下载 |
| 来源 | [V1.1采纳:S-002]（依据 yt-dlp 2025-09-23 公告） |

---

## V1.0 → V1.1 闭环记录

| 编号 | V1.0 开放问题 | V1.1 闭环结果 | 闭环来源 |
|------|---------------|---------------|----------|
| Q-001 | B 站反爬与登录墙边界 | ✅ 闭环：实测 80% 可下；F-012 默认 cookie 模板 + E_DL_BILI_403 错误码 | R-001 + S-001 |
| Q-002 | deepseek-v4-flash context | ✅ 闭环：实测支持 1M context（远超 30 分钟 8-15k）；风险等级高→低 | R-003 + S-003 |
| Q-003 | fast-whisper 中文 CER | ✅ 闭环：标准普话 CER 4-8%（92-96% 准确率）；风险中→低 | R-004 + S-004 |
| Q-004 | raw/ schema 冲突 | ✅ 闭环：`yaml.safe_load` 宽容；`video_` 前缀隔离；风险中→低 | R-005 + S-005 |
| Q-005 | DD-001 模块编号 | ✅ 部分闭环：默认按 4 类编号；最终由 SA/DD 在下游棒产出 | R-007 + 上下游协作 |
| Q-006 | 缓存粒度 | ✅ 闭环：升级为 `sha256(url) + etag/last-modified` 双键 | R-102 + S-102 |
| Q-007 | LLM 单次调用预算 | ✅ 闭环：PM 决策选项 A（单次整合）：扩为 ≤ 20k input / 4k output | R-103 + S-008 |

**V1.0 → V1.1 闭环率**：7/7 = **100%**。

---

## 待确认项分布统计（V1.1）

| 风险等级 | 数量 | 占比 |
|----------|------|------|
| 高 | 1 | 100%（Q-008 Deno 必装） |
| 中 | 0 | 0% |
| 低 | 0 | 0% |
| **合计** | **1** | **100%** |

| 状态 | 数量 |
|------|------|
| V1.0 已闭环 | 7 |
| V1.1 新增 | 1 |
| 待 SA/DD 协作 | 0 |
| 升级为 backlog | 0 |

---

## 闭环流程（V1.1）

1. RA-001 在 V1.0 调研阶段对 7 条 Q-NNN 逐一回写（实测数据/官方文档/官方 API 响应）—— **V1.1 全部完成**
2. 每条 Q-NNN 状态变为"已闭环"—— **V1.0 7 条已闭环**
3. 升级为 backlog 的 Q-NNN 在 `backlog.md` 中登记 —— **V1.1 无 backlog 升级**
4. 本 PRD 升级为 V1.1，最终 CCI = 0.960 ≥ 0.93（V1.0 CCI = 0.925 → V1.1 CCI = 0.960，提升 0.035）
5. V1.1 新增 Q-008（Deno 必装）—— 由第⑧棒 CLI 启动时自动检测并打印安装命令，无需 RA 二次调研

---

> **清单结束**。V1.1 待确认项 1 条（Q-008），≤10 合规。
