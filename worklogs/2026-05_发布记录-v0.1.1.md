# 2026-05 — 收束节点 v0.1.1 发布记录

> 原 CHANGELOG.md 整合至此。v0.1.0 内容回溯整理。

---

## [0.1.1] — 2026-05

### Changed
- 全项目 `print()` 迁移至 `logging` 模块，新增 `src/common/logging_config.py`
- 所有 `except Exception` 裸捕获改为 `except Exception as exc` + 日志记录

### Fixed
- 移除 `config.yaml` 和 `tests/fixtures/p2test_config.yaml` 中硬编码的 API Key
- 修复 `stages/__init__.py` 中 5 个不存在的函数级导入
- 修复 `src/infrastructure/stages/organizer.py` 中 logger 定义位置导致的 E402 错误

### Added
- ruff lint 配置（`pyproject.toml`）
- `.pre-commit-config.yaml`：ruff + gitleaks
- `CLAUDE.md`、`meta/CODE_MAP.md`、`STATUS.md`
- ADR 0001：六阶段管道文件系统通信
- `docs/reports/收束报告-v0.1.1.md`

### Security
- 移除仓库中泄露的 DeepSeek / Tavily API Key

---

## [0.1.0] — 2026-04

### Added
- 六阶段管道引擎（Collect → Deepen → Clean → Extract → Organize → Report）
- 10 个搜索后端（DDG/OpenAlex/Crossref/arXiv/S2/PubMed/Wikipedia/GitHub/GoogleNews/Tavily）
- LLM 抽象层（DeepSeek/OpenAI/Anthropic/Ollama）
- Typer CLI（7 命令）+ Gradio Web 界面
- Python SDK（`research()` / `quick_collect()`）
- PDF 摄取（MinerU 集成）
- 反偏差深挖（实体拆分 + 画像注入 + 缺口检测 + 同名消歧）
- 反向传播（知识树质量评估循环）
- P1/P2 功能增强
